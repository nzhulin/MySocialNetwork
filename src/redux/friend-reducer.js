let initialState = {
    friends: [
        { id: 1, name: 'Nikolay' },
        { id: 2, name: 'Andrey' },
        { id: 3, name: 'Oleg' },
    ],
};


const friendReducer = (state = initialState, action) => {

    return state;
}

export default friendReducer;